import { MoneyColorPipe } from './money-color.pipe';

describe('MoneyColorPipe', () => {
  it('create an instance', () => {
    const pipe = new MoneyColorPipe();
    expect(pipe).toBeTruthy();
  });
});
